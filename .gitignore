# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfplan
terraform/tfplan
terraform/.terraform.lock.hcl

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
ENV/
env/
.pytest_cache/
.hypothesis/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
.e2e-checkpoint

# Secrets
*.env
.env.*
secrets/


openia.7m.key
openia.7m.key

scripts/bench_embedding_rs/target/

# .NET build output
bin/
obj/
# …but keep emulator shims for hooks tests
!tests/emu/bin/
!tests/emu/bin/**

# Build artifacts
*.tar.gz

# Test files
test_*.pdf
make_test_pdfs.py

# Data exports
*_dev3.json
dev3_*.json
skf_*.json
id_mappings.json

# Windows
nul
.lock-hash

helm/omnivec/.last-deploy-fingerprint


# .NET publish artifacts
connectors/worker/dotnet/publish/

# CLI build outputs — release binaries should ship via GH Releases, not in repo
cli/omnivec
cli/omnivec.exe
cli/omnivec-cli.exe
cli/omnivec-darwin-*
cli/omnivec-linux-*
cli/omnivec-windows-*
cli/bin/
dist/
dist-v*/

# One-off / scratch scripts and data dumps
cli/check_dest.py
cli/purge_and_verify.py
items.json
scripts/cli-blob-demo.ps1
scripts/cli-blob-pdf-demo.ps1
scripts/cli-demo.ps1

# Local azd logs
.azd-logs/

# Security scan results (uploaded to Security tab via workflow, not committed)
*.sarif

security-report.html
